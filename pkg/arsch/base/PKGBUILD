# Maintainer: A.M.Price <amprice@mailbox.org>
pkgname=arsch-base
pkgver=0.0.1
pkgrel=1
pkgdesc="Base system configuration for arsch system"
arch=('any')
url="https://raw.githubusercontent.com/ashprice/arsch/master/x86_64"
license=('MIT')
groups=(arsch)

provides=(vim vi)
conflicts=(vim vi)
replaces=(vim vi)
install=arsch-base.install

#Base group
depends=(
	bash bzip2 cryptsetup device-mapper dhcpcd diffutils e2fsprogs file filesystem findutils gawk gcc-libs gettext glibc grep gzip inetutils iproute2 iputils less licenses linux linux-firmware logrotate lvm2 man-db man-pages mdadm pacman pciutils perl procps-ng psmisc s-nail sed shadow sysfsutils systemd-sysvcompat tar texinfo usbutils util-linux
)

depends+=(
	sudo wget fzf unzip zip lrzip dialog pacman-contrib ncdu
)

depends+=(
	htop vnstat
)

depends+=(
	zsh fish
)

depends+=(
	exfat-utils dosfstools
)

depends+=(
	neovim
)

rootdir=$PWD

#package() {
#	cp -a "$rootdir/files/"* "$pkgdir"
#	chmod -R 0750 "$pkgdir/etc/sudoers.d"
#}
